on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: ./bootstrap
      - run: ./configure --with-utils=host
      - run: make
